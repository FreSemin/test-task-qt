export const APP_PREFIX = 'api';

export const APP_PORT = 4000;

export const APP_DOCKER_PORT = 4000;

export const APP_LISTEN_PORT = (port: number) => `APP listening on port ${port}`;

export const APP_LISTEN_DOCKER_PORT = (port: number) => `APP docker external port ${port}`;

export const APP_DATABASE_TYPE = 'postgres';

export const APP_DATABASE_PORT = 5432;
