// ENV
export enum NODE_ENV {
    DEV = 'development',
    PROD = 'production',
}

// APP
export const BEARER = 'Bearer';

// REDIS
export const DEFAULT_REDIS_TTL = '1h';

// JWT
export const DEFAULT_TOKEN_EXPIRE_TIME = '15m';
export const DEFAULT_REFRESH_TOKEN_EXPIRE_TIME = '24h';
